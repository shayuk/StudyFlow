{
  "version": 2,
  "framework": "vite",
  "buildCommand": "pnpm install && pnpm build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "functions": {
    "api/**/*.{js,ts}": {
      "maxDuration": 30
    }
  }
}
