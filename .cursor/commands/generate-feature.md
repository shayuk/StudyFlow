# generate-feature

Generate all necessary files for a new feature, but place them only in correct domain folders.
Do not overwrite or duplicate existing files.
Verify naming conventions and TypeScript types before generating.

This command will be available in chat with /generate-feature
