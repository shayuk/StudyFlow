# 🚨 תיקון דחוף - האתר מציג קוד במקום UI

## הבעיה:
Vercel מבולבל וחושב שהפרויקט שלך הוא שרת Node.js במקום React app.

## תיקון מיידי - 5 דקות:

### שלב 1: תיקון הגדרות הפרויקט ב-Vercel

1. **פתח את Vercel Dashboard:**
   - לך ל: https://vercel.com
   - התחבר
   - לחץ על הפרויקט **studyflow-ui**

2. **לך להגדרות Build:**
   - לחץ על **Settings** (למעלה)
   - בתפריט השמאלי לחץ על **General**
   - גלול למטה ל-**Build & Development Settings**

3. **שנה את ההגדרות הבאות:**
   ```
   Framework Preset: Vite
   Build Command: pnpm build
   Output Directory: dist
   Install Command: pnpm install
   Root Directory: ./
   ```

4. **לחץ Save**

### שלב 2: נקה את ה-Cache

1. באותו מסך Settings
2. גלול למטה ל-**Advanced**
3. לחץ על **Delete Build Cache**
4. אשר את המחיקה

### שלב 3: הגדר משתנה סביבה

1. לחץ על **Environment Variables** בתפריט השמאלי
2. ודא שקיים:
   ```
   VITE_API_BASE_URL = https://study-flow-server-one.vercel.app
   ```
3. אם לא קיים - הוסף אותו

### שלב 4: פרוס מחדש

1. לחץ על **Deployments** (למעלה)
2. לחץ על שלוש הנקודות (...) ליד הפריסה האחרונה
3. לחץ **Redeploy**
4. בחלון שנפתח:
   - ✅ סמן "Use existing Build Cache" - **לא! תוריד את הסימון**
   - לחץ **Redeploy**

### שלב 5: המתן וב

1. חכה 2-3 דקות לפריסה
2. פתח: https://studyflow-ui.vercel.app/
3. אתה אמור לראות את האפליקציה ולא קוד!

---

## אם עדיין לא עובד:

### אופציה א: צור פרויקט חדש
1. מחק את הפרויקט הנוכחי ב-Vercel
2. צור פרויקט חדש
3. בחר **Import Git Repository**
4. בחר את הריפו שלך
5. **חשוב:** בחר **Vite** בתור Framework Preset
6. הגדר את המשתנה VITE_API_BASE_URL

### אופציה ב: בדיקת קבצים
הרץ את הפקודות האלה אצלך:
```cmd
pnpm build
dir dist
```

אתה אמור לראות קבצי HTML, JS, CSS בתיקיית dist.

---

## למה זה קרה?

Vercel ראה את תיקיית `server` וחשב שזה פרויקט Node.js.
התיקונים שעשינו:
1. הוספנו `vercel.json` עם הגדרות Vite
2. הוספנו `.vercelignore` שמתעלם מתיקיית server
3. הגדרנו את ה-Framework כ-Vite

---

## עדיין תקוע?
שלח לי screenshot של:
1. מסך Settings > General > Build & Development Settings
2. השגיאה שאתה רואה
3. הלוגים מ-Vercel (Functions > Logs)
